Set-Location "C:\dev\express-automation-app"

Set-Content -Path "package.json" -Value (Get-Clipboard)

git add package.json
git commit -m "Update package.json with correct start script and dependencies"
git push

